# Technisches und anwenderbezogenes Review — Poise Setpoint Thermostat

*Stand: 12. Juli 2026 — Review des Commits `61f213e9af3cd3228df0eb6ea8883655ce649b6d`*

---

## 1. Untersuchungsstand

| Merkmal | Wert |
| --- | --- |
| Repository | `Ecronika/poise-thermostat` (GitHub) |
| Untersuchter Stand | Commit `61f213e9af3cd3228df0eb6ea8883655ce649b6d`, 2026-07-12 10:39 +0200 |
| Branch-Hinweis | Der Review-Arbeitsbranch `claude/poise-thermostat-review-3r66jz` zeigt auf denselben Snapshot wie der aktuelle Default-Branch `main` (CI-Lauf #1093 bestätigt `61f213e` auf `main`). Die Historie besteht ausschließlich aus „Add files via upload“-Snapshots ohne durchgängige Commit-Historie; lokale `main`-Klone können dadurch veralten. |
| Version (`manifest.json`) | **0.163.0** (identisch in `pyproject.toml`, `const.py`, `card/package.json`, README-Badge) |
| Home-Assistant-Minimum | 2025.1.0 (`hacs.json`) |
| Python-Anforderung | ≥ 3.12 (`pyproject.toml`); CI-Matrix 3.12 + 3.13 |
| Releases | **Keine** (GitHub Releases leer, keine Tags) |
| Offene Issues | 0 (Issue-Liste leer) |
| Pull Requests | Keine auffindbar (API-Abruf der PR-Liste lieferte 404; Entwicklung erfolgt offenbar per Datei-Upload direkt auf `main`) |
| Letzte CI-Läufe | #1093 auf `61f213e` (HEAD): **success**; #1092 auf `febd67c`: failure (eine Minute später durch #1093 ersetzt); insgesamt 1093 CI-Läufe |
| ADRs | 61 Dokumente (`docs/adr/ADR-0000` … `ADR-0060`) |

### Lokal tatsächlich ausgeführte Prüfungen (Python 3.13.12, HA 2024.12.5 via `pytest-homeassistant-custom-component==0.13.195`, Node 22.22.2)

| Prüfung | Ergebnis |
| --- | --- |
| `ruff check .` | ✅ bestanden (0 Findings) |
| `ruff format --check .` | ✅ 226 Dateien formatkonform |
| `mypy` (strict) | ⚠️ **21 Fehler in 8 Dateien** — nur in HA-Glue-Modulen, ausschließlich sichtbar, wenn Home Assistant real installiert ist (Re-Export-/`type: ignore`-/`via_device`-Typfehler). In der CI läuft der mypy-Job **ohne** installiertes HA (`ignore_missing_imports`), dort grün. Der Anspruch „mypy --strict, all modules“ gilt also uneingeschränkt nur für den HA-freien Kern. |
| Pure-Core-Tests | ✅ **742 passed**, Coverage 97,86 % (Gate 85 %) |
| HA-Integrationstests | ✅ **163 passed**; Glue-Coverage lokal 94,70 % — **verfehlt das 95-%-Gate knapp** (CI auf GitHub war grün; umgebungsabhängige Abdeckung, lokal HA 2024.12.5) |
| Card: `tsc --noEmit` | ✅ 0 Fehler |
| Card: `node --test` | ✅ 49 Tests bestanden |
| Card-Build | ✅ Build reproduziert das eingecheckte Bundle byte-identisch (`poise-card.js 0.163.0`) |
| Hassfest / HACS-Validierung | Nicht lokal ausgeführt (Actions-Container); in CI-Lauf #1093 als Job „Validate“ grün |

**Nicht ausführbar/nicht geprüft:** Feldverhalten an realer Hardware (kein TRV/Klimagerät im Container), Langzeit-Regelgüte, HA-2025.x-Laufzeit (lokal nur HA 2024.12.5 verfügbar — älter als das deklarierte Minimum 2025.1.0; die Integrationstests liefen dennoch durch).

> **Platzhalter:** Abschnitte 2–10 werden nach Abschluss der Tiefenanalysen (Regelungsphysik, Sicherheitskette, ADR-Audit, Testqualität, Anwendersicht, Community-Recherche) ergänzt.
