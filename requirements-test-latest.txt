# HA-LATEST integration-test harness (review plan E.11): the newest RELEASED
# pytest-homeassistant-custom-component as an early-warning target against HA
# API drift. Same shape as requirements-test.txt (see there for the
# frontend-wheel rationale); bump both files deliberately.
#
# pytest-hacc 0.13.316 pins homeassistant==2026.2.3 and runs on Python 3.13;
# the frontend build is the one HA 2026.2.3's frontend manifest declares.
# NOTE: pytest-hacc releases currently stop at HA 2026.2.3 (master tracks
# 2026.8 unreleased). Newer HA needs Python >= 3.14 — when a newer harness
# release appears, bump this file AND the latest-job python-version in ci.yml.
pytest-homeassistant-custom-component==0.13.316
home-assistant-frontend==20260128.6

pytest-cov
