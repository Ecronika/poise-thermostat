# Pinned developer/CI tooling for a reproducible quality gate (review P1).
# Static-analysis gates are pinned exactly so a new tool release cannot
# silently change lint/type results; test runners are bounded to a major
# that supports the Python 3.12/3.13 matrix.
ruff==0.15.18
mypy==1.13.0
pytest>=8.0,<9
pytest-cov>=5,<8
# F.5: property-based + state-machine tests over the pure control core.
# Bounded to a major like the runners; the CI profile is derandomised
# (tests/conftest.py), so a new hypothesis release cannot make the gate flaky.
hypothesis>=6.100,<7
